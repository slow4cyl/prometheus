diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index abb70b543..69d9c3d82 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -187,9 +186,11 @@ KANBAN_GUIDANCE = (
     "You have been assigned ONE task from "
     "the shared board at `~/.hermes/kanban.db`. Your task id is in "
     "`$HERMES_KANBAN_TASK`; your workspace is `$HERMES_KANBAN_WORKSPACE`. "
-    "The `kanban_*` tools in your schema are your primary coordination surface — "
-    "they write directly to the shared SQLite DB and work regardless of terminal "
-    "backend (local/docker/modal/ssh).\n"
+    "The `kanban_*` tools in your schema are your coordination surface for "
+    "task lifecycle (show, heartbeat, complete, block, comment). "
+    "You ALSO have full access to terminal, web_search, browser, file I/O, "
+    "delegate_task, and all other standard tools — use them freely. "
+    "Do NOT block a task for lack of tools you actually have.\n"
     "\n"
     "## Lifecycle\n"
     "\n"
